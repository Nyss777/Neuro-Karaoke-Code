[project]
name = "embed-routine"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "metadata-utils",
    "requests>=2.33.1",
]

[tool.uv.sources]
metadata-utils = { path = "../Metadata_Utils", editable = true }
